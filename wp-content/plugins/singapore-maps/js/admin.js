/* 
* Admin script for "Singapore Maps" Wordpress plugin
*/

jQuery(document).ready(function() {

	// Setup MiniColors plugin
	jQuery('#minicolors_normal').minicolors({
		defaultValue:'#f38713'
	});
	jQuery('#minicolors_hover').minicolors({
		defaultValue:'#cc4900'
	});
	jQuery('#minicolors_font').minicolors({
		defaultValue:'#ffffff'
	});	
	
});